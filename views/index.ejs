<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>To Do List App</title>
</head>
<body>
    <h1>To Do List App</h1>

    <form action="/task" method="post">
        <input type="text" placeholder="Add Your Task Here" name="task" id="taskInput">
        <button type="submit">Submit</button>
    </form>
    
    <section data-postion="tasks">
        <h2>Tasks</h2>
        <ul class="tasks">
            <% for(let i = 0; i < tasks.length; i++) {%>
                <div class="container">
                <li class="task">
                    <span><%= tasks[i].task %></span>
                </li>
                
                <section data-postion="delete-task">
                    <button class="mark-done" data-task-id="<%= tasks[i]._id %>" style="margin-left:  2rem;">DELETE</button>
                </section>
                </div>


                <%  } %>
            </ul>
        </section>

        <script src="/main.js"></script>
        <!-- <script src="../public/main.js"></script> -->
</body>
</html>
